# Kanboard 插件扩展能力补齐说明 V0.8.24

## 本轮目标
继续对照 Kanboard 官网补齐插件开发层的系统能力。本轮不再只展示 Plugin.php 骨架，而是把插件能够扩展的三类关键能力产品化：认证提供者、自动动作、通知类型。

## 官网依据
- Authentication Architecture：Kanboard 支持本地/LDAP 用户名密码、OAuth2、反向代理、Remember Me、TOTP 2FA 等认证方式，并按 SessionCheck、Pre、Password、OAuth、Post、UserProvider 同步的顺序执行。参考：https://docs.kanboard.org/v1/plugins/authentication_architecture/
- Authentication Providers：插件可在 `initialize()` 中通过 `AuthenticationManager` 注册 provider，provider 需要实现预定义认证接口，并返回实现 `UserProviderInterface` 的用户对象。参考：https://docs.kanboard.org/v1/plugins/authentication_providers/
- Adding Automatic Actions：自定义动作类需要继承 `Kanboard\Action\Base`，实现描述、兼容事件、动作参数、事件参数、条件判断和 `doAction()`。参考：https://docs.kanboard.org/v1/plugins/automatic_actions/
- Add Notification Types with Plugins：插件可通过 user/project notification type model 注册新的通知类型，通知处理器需要实现 `NotificationInterface`。参考：https://docs.kanboard.org/v1/plugins/notifications/

## 本轮新增
- 顶部新增 `扩展能力` 入口，位于 `开发者` 与 `运维任务` 之间，表达插件开发从骨架进入具体系统扩展点。
- 新增 `state.extensions.authProviders`，记录自定义认证 provider 的接口类型、类名、外部 ID 字段、自动创建用户、外部组同步、SessionCheck 和 2FA 后置检查。
- 新增认证执行流程视图，展示 SessionCheck、Pre、Password、OAuth、Post 和本地同步的执行顺序。
- 新增已知 provider 列表，区分 Kanboard native provider 与插件 provider，并支持模拟注册自定义 provider。
- 新增认证 provider 代码预览，展示 `AuthenticationManager` 注册和 `UserProviderInterface` 相关字段。
- 新增 `state.extensions.automaticActions`，记录动作类名、兼容事件、自定义事件、动作必填参数、事件必备参数、触发条件和执行日志。
- 新增自动动作代码预览，展示 `getCompatibleEvents()`、`getActionRequiredParameters()`、`getEventRequiredParameters()`、`hasRequiredCondition()`、`doAction()`、`register()` 和 `addEvent()`。
- 新增 `state.extensions.notificationTypes`，记录通知类型 key、显示名称、处理器 class、适用范围、测试事件、endpoint 和投递日志。
- 新增通知类型代码预览，展示 `NotificationInterface`、`notifyUser()`、`notifyProject()` 和 user/project `setType()` 注册。
- 新增扩展风险检查，覆盖认证类名、OAuth 外部 ID、自动用户同步、SessionCheck、自动动作参数、事件上下文、通知 key、通知处理器和 endpoint。
- 将 localStorage key 升级为 `kanboard-static-v0824`，并将项目 JSON 导出版本同步为 `kanboard-static-v0824`。

## 产品判断
- `插件`、`开发者`、`扩展能力` 是三个不同层次：插件管理负责安装更新，开发者入口负责 Webhook/API/Plugin.php，扩展能力负责解释插件能改造 Kanboard 的哪些核心机制。
- 认证 provider 是实例级安全能力，需要和用户同步、外部 ID、SessionCheck、2FA 放在一起表达，否则只显示 OAuth/LDAP 会漏掉认证架构的关键顺序。
- 自动动作是 Kanboard 的高价值扩展点，产品表达必须包含事件、参数、条件和执行结果，而不是只显示“自动化规则”。
- 通知类型扩展要同时区分 user 和 project 两种通知范围，因为官网明确这两类通知的配置入口不同。
- 本轮仍是静态复现，不真实加载 PHP 插件、不执行认证、不向外部 endpoint 发送网络请求，只表达配置、代码骨架、风险和模拟日志。

## 验收结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 287 项检查。
- 生成截图：`设计图/kanboard-v0.8.24-extension-lab.png`。

## 下一步
继续核对 Kanboard 官网剩余插件开发与系统能力：授权架构、Avatar providers、Custom Forms、Custom Group Providers、Custom Routes、External Link Providers、External Task Providers、Mail Transport、Plugin Overrides、Schema Migrations、Registering Helpers、Using Events，以及平台化安装教程是否还需要静态复现。
