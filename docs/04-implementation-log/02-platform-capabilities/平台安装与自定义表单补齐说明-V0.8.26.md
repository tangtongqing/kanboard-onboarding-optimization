# 平台安装与自定义表单补齐说明 V0.8.26

## 本轮目标
继续收敛 Kanboard 官网剩余差距。本轮补齐两个上一版明确留下的缺口：插件侧的 Custom Forms，以及部署侧的 Debian / Ubuntu / RHEL / Windows 平台化安装教程。

## 本轮新增
- 将 localStorage key 升级为 `kanboard-static-v0826`，项目 JSON 导出版本同步为 `kanboard-static-v0826`。
- 在“部署检查”中新增 `state.deployment.platform`：
  - 支持 Debian + Apache、Ubuntu + Apache、Ubuntu + Nginx/PHP-FPM、RHEL/CentOS + Apache、Windows + Apache、Windows + IIS。
  - 增加系统包、Web Server、PHP 运行方式、源码解压、data 权限、每日任务、SELinux、Windows Runtime、IIS Rewrite 等检查项。
  - Runbook 预览新增平台安装片段，按选择的平台生成 package command、service、data owner、daily job 和特殊依赖。
- 在“扩展能力”中新增 `state.extensions.customForms`：
  - 支持 modal 入口、Controller/Action/Plugin、上下文参数、重定向、模板、字段配置、CSRF、单 form、必填校验和错误回显。
  - 增加 Custom Form 代码预览，覆盖 modal helper、form helper、csrf、字段渲染、controller POST、失败回显和成功重定向。
  - 增加 Custom Form 风险检查和模拟提交日志。

## 产品判断
- 平台安装不应该只是文档链接；对管理员来说，真正需要的是“当前实例是否完成包、服务、权限、后台任务和特殊平台依赖”。
- Debian/RHEL 文档有时效提示，本轮在 UI 中把“需要复核发行版包名”作为风险，而不是假设文档永远新鲜。
- Custom Forms 是插件开发中的完整小流程：打开 modal、渲染表单、校验、回显错误、成功后 redirect，缺一环都会变成典型原型逻辑漏洞。
- Windows / IIS 不是主推路径，但官网列为可用安装环境，因此在静态复现中保留特殊依赖检查。

## 官网依据
- Kanboard Custom Forms: https://docs.kanboard.org/v1/plugins/forms/
- Kanboard Installation Instructions: https://docs.kanboard.org/v1/admin/installation/
- Kanboard Requirements: https://docs.kanboard.org/v1/admin/requirements/
- Kanboard Debian Installation: https://docs.kanboard.org/v1/admin/debian/
- Kanboard Ubuntu Installation: https://docs.kanboard.org/v1/admin/ubuntu/
- Kanboard RHEL/CentOS/Oracle Linux Installation: https://docs.kanboard.org/v1/admin/rhel/
- Kanboard Windows Server Installation: https://docs.kanboard.org/v1/admin/windows/

## 验证结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 320 项检查。
- 已生成截图：
  - `设计图/kanboard-v0.8.26-platform-install.png`
  - `设计图/kanboard-v0.8.26-custom-forms.png`
