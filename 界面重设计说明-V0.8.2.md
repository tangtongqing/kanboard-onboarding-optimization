# 界面重设计说明 V0.8.2

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-02 |
| 主题 | Open-source Command Desk：开源工具指挥台 |
| 目标 | 在不改变功能逻辑的前提下，重塑静态页面视觉系统，进一步降低 AI 模板感 |
| 范围 | `styles.css`、README、版本记录、设计截图 |
| 使用 Skill | `frontend-design`、`baseline-ui`、`kpi-dashboard-design`、`design-spells`、`fixing-accessibility` |

## 设计方向

这次不把 Kanboard 做成炫技界面，而是让它更像一个可长期工作的项目管理工具。关键词是：

- 工具台感：信息要密、边界要清楚、操作要稳定。
- 开源气质：不过度品牌化，不过度营销化，保留朴素和透明感。
- 状态感：项目、指标、列、卡片都能快速识别当前状态。
- 克制记忆点：有记忆点，但不靠装饰堆叠。

如果去掉 Kanboard 标识，这版界面的识别点应该是：深色左侧项目导航、白色选中项目块、顶部状态指标条、看板列顶部状态色、任务卡片左侧状态线。

## DFII 评估

| 维度 | 分数 | 判断 |
|---|---:|---|
| Aesthetic Impact | 4 | 比上一版更有工具台识别度，但不过度表现 |
| Context Fit | 5 | 适合 Kanboard 这种开源项目管理工具 |
| Implementation Feasibility | 5 | 主要用 CSS 完成，不破坏功能逻辑 |
| Performance Safety | 5 | 没有重动画、滤镜、图片依赖 |
| Consistency Risk | 2 | 新视觉系统仍然基于现有组件结构，维护风险较低 |

DFII = 4 + 5 + 5 + 5 - 2 = 12，适合完整执行。

## 主要改动

| 区域 | 调整 | 设计理由 |
|---|---|---|
| 全局视觉 | 建立中性灰工作区、深色导航、蓝色主操作、绿色/橙色/蓝色/绿色状态色 | 避免单一主色造成模板感 |
| 侧栏 | 增加品牌短竖条，当前项目改成白色选中块 | 形成清楚的导航焦点和截图记忆点 |
| 顶部区域 | 顶部信息和操作按钮归入统一白色工作台面板 | 让页面更像真实后台工具，而不是散落组件 |
| 指标区 | 改成状态条布局，标签靠左、数字靠右、顶部色条区分状态 | 使用 KPI 仪表盘的快速扫读模式 |
| 工具区 | 搜索、筛选、视图控制继续保持独立容器，但密度更紧 | 强化“操作台”而不是“展示卡片” |
| 泳道 | 增加左侧竖向状态标识 | 让用户更快区分当前泳道块 |
| 看板列 | 每列顶部加入状态色：待办、进行中、待验证、已完成 | 列本身成为工作流状态，而不是普通容器 |
| 任务卡 | 左侧状态线 + 轻阴影 + 小状态点 + hover 轻反馈 | 保留可操作性，同时减少模板化卡片感 |
| 标签/按钮 | 降低圆角和胶囊感，收紧间距 | 更贴近后台工具而非营销页 |
| 可访问性 | 保留焦点态、减少动画、检查关键颜色对比 | 保证视觉优化不牺牲可用性 |

## 验收结果

本轮执行：

```powershell
node tests/static-feature-audit-v08.js
git diff --check
```

结果：

- 79 项静态功能回归全部通过。
- 390px 移动端宽度无页面级横向溢出。
- 关键颜色对比检查通过 4.5:1：主文本、弱文本、主按钮、侧栏文字、危险操作、状态标签。
- 生成截图：`设计图/kanboard-v0.8.2-command-desk-redesign.png`。

## 学习点

这轮“去 AI 味”的核心不是加更多花哨视觉，而是建立一套能解释的视觉规则：

- 状态色只用于状态，不到处乱用。
- 卡片更轻，列更像工作流容器。
- 当前项目、当前泳道、当前列状态都有明确识别方式。
- 界面记忆点来自结构和状态，不来自装饰。

下一步进入 Figma 时，可以把 V0.8.2 当成优化后高保真的视觉基准。
