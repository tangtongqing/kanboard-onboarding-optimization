# 系统认证与安全配置补齐说明 V0.8.19

## 本轮目标

本轮继续补齐 Kanboard 官网中的系统级配置能力，重点覆盖 API 认证、LDAP、反向代理 SSO、安全策略和 `config.php` 配置预览。它和 V0.8.18 的全局用户/用户组管理相互衔接：V0.8.18 管“实例里有哪些身份”，V0.8.19 管“这些身份如何被认证、保护和接入外部系统”。

## 官网依据

- API 认证文档说明：Kanboard API endpoint 是 `jsonrpc.php`；默认使用 HTTP Basic；应用 API 使用 `jsonrpc` 和设置页 API token；用户 API 使用用户名和密码或个人访问令牌；也可以通过 `API_AUTHENTICATION_HEADER` 配置自定义 Header。
- API 文档说明：从 Kanboard v1.2.8 起，启用两步验证的用户必须使用 API Key。
- LDAP 文档说明：Kanboard 支持 anonymous、proxy、user 三种 LDAP bind 方式，支持用户筛选、自动创建 LDAP 用户、LDAP 用户组同步。
- 反向代理认证文档说明：Kanboard 可以信任反向代理传入的认证 Header；启用该方式时需要配置可信代理网络，并防止客户端伪造认证 Header。
- 配置文件文档说明：`config.php` 可配置 LDAP、反向代理认证、API Header、隐藏登录表单、禁用登出、Markdown HTML 转义、私网访问等选项。
- 安全文档说明：安装后应修改默认账号密码，不应直接暴露到不可信网络；反向代理应清理传入 Header；暴力破解保护在 3 次失败后出现 CAPTCHA、6 次失败后锁定账号 15 分钟；插件和外链/Webhook 等远程 URL 能力需要警惕 SSRF。

参考链接：
- https://docs.kanboard.org/v1/api/authentication/
- https://docs.kanboard.org/v1/admin/ldap/
- https://docs.kanboard.org/v1/admin/reverse_proxy_authentication/
- https://docs.kanboard.org/v1/admin/config/
- https://docs.kanboard.org/v1/admin/security/

## 本轮新增

- 顶部新增 `系统设置` 入口。
- 新增 `state.system.api`，记录 API endpoint、应用 API 用户、API token、自定义认证 Header 和 2FA 用户 API Key 要求。
- 新增 `state.system.ldap`，记录 LDAP 开关、服务器、bind type、User Base DN、User Filter、自动创建用户、用户组同步、Group Base DN 和 Group Filter。
- 新增 `state.system.reverseProxy`，记录反向代理认证开关、可信代理网络、用户/邮箱/姓名 Header、默认管理员、默认邮箱域名和 Header 清理状态。
- 新增 `state.system.security`，记录隐藏登录表单、禁用登出、Markdown HTML 转义、暴力破解阈值、锁定时长、外链/Webhook 私网访问开关。
- 新增系统设置摘要卡：API 认证方式、LDAP 状态、反向代理状态、安全提示数量。
- 新增风险提示：反向代理缺少可信网络、未清理认证 Header、隐藏登录表单但未启用外部认证、允许私网访问、2FA 用户未要求 API Key、LDAP 缺少 User Base DN。
- 新增 `config.php` 静态预览，自动生成与当前开关对应的配置片段。
- 将 localStorage key 升级为 `kanboard-static-v0819`，将项目 JSON 导出版本同步为 `kanboard-static-v0819`。

## 产品判断

系统认证配置不是普通表单设置，而是会影响实例访问入口、安全边界和运维交接的后台能力。因此本轮没有把 LDAP、反向代理和 API 分散到用户管理里，而是集中成一个实例级“系统设置”入口。

这一层的产品重点是“前置条件可见”：

1. API 认证要能看出是 Basic 还是自定义 Header。
2. LDAP 要能看出是否允许自动创建用户和同步组。
3. 反向代理认证必须和可信代理网络、Header 清理放在一起。
4. 安全配置要把暴力破解阈值、登录入口、私网访问风险放在同一张图里。
5. `config.php` 预览让静态原型不仅像界面，也能解释真实部署时配置落在哪里。

## 静态复现边界

- 不调用真实 API。
- 不保存真实 API token。
- 不连接 LDAP 服务。
- 不处理真实反向代理 Header。
- 不执行真实登录、登出或验证码流程。
- 不做真实 SSRF 防护或网络访问控制。
- 只在静态页面中模拟配置结构、风险状态和 `config.php` 片段。

## 验收结果

- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 188 项检查。
- 已生成 `设计图/kanboard-v0.8.19-system-settings-default.png`。
- 已生成 `设计图/kanboard-v0.8.19-auth-risk-config.png`。
- 已生成 `设计图/kanboard-v0.8.19-config-preview.png`。

## 下一步

继续补齐 Kanboard 剩余系统级能力：管理员 CLI、Cronjob、后台任务、邮件配置、数据库/性能配置等。Figma 高保真原型继续放在静态复现差距进一步收敛之后。
