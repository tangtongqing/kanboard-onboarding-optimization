# 项目1版本记录

## V0.8.26 - 平台安装与自定义表单补齐
| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-08 |
| 状态 | 已完成 |
| 主题 | 根据 Kanboard Custom Forms、Installation、Requirements、Debian、Ubuntu、RHEL/CentOS/Oracle Linux 和 Windows Server 安装文档补齐剩余插件表单与平台安装能力 |
| 主要文件 | `app.js`、`index.html`、`styles.css`、`tests/static-feature-audit-v08.js`、`平台安装与自定义表单补齐说明-V0.8.26.md`、`设计图/kanboard-v0.8.26-platform-install.png`、`设计图/kanboard-v0.8.26-custom-forms.png`、`README.md` |

### 本轮新增
- 新增 `state.deployment.platform`，覆盖 Debian + Apache、Ubuntu + Apache、Ubuntu + Nginx/PHP-FPM、RHEL/CentOS + Apache、Windows + Apache、Windows + IIS。
- 在 `部署检查` 弹窗新增平台安装教程区，覆盖系统包、Web Server、PHP 运行方式、源码解压、data 权限、每日任务、SELinux、Windows Runtime 和 IIS Rewrite。
- 部署 Runbook 新增平台安装片段，按当前平台生成 package command、service、data owner、daily job 和特殊平台依赖。
- 新增 `state.extensions.customForms`，覆盖 modal 入口、Controller/Action/Plugin、上下文参数、重定向、模板、字段配置、CSRF、单 form、必填校验和失败回显。
- 在 `扩展能力` 弹窗新增 Custom Forms 配置区、代码预览、风险检查和模拟提交日志。
- 将 localStorage key 升级为 `kanboard-static-v0826`，并将项目 JSON 导出版本同步为 `kanboard-static-v0826`。

### 关键判断
- 平台安装文档属于实例部署能力，应该放进 `部署检查`，而不是放进插件或项目设置。
- Debian/RHEL 文档存在时效提示，静态原型应该把“发行版包名需复核”显性化为风险。
- Custom Forms 是完整表单闭环，不是简单新增字段：必须同时表达 modal、form helper、POST 处理、校验失败回显和成功 redirect。
- Windows/IIS 不是推荐主路径，但官网列为支持环境，因此以风险检查方式覆盖其特殊依赖。

### 验证结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 320 项检查。
- 生成 V0.8.26 平台安装教程截图。
- 生成 V0.8.26 Custom Forms 插件表单截图。

### 下一版本
继续核对 API Reference 中更细的 procedure、管理员信息架构、配置文件细项和数据库文档是否还有需要进入静态复现的内容；待官网差距进一步收敛后进入 V0.9 Figma 高保真原型。

## V0.8.25 - 插件进阶扩展补齐
| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-08 |
| 状态 | 已完成 |
| 主题 | 根据 Kanboard Authorization、Custom Routes、Avatar、Group Provider、External Link/Task Provider、Mail Transport、Schema Migrations、Helpers、Events 和 Overrides 文档补齐插件进阶扩展能力 |
| 主要文件 | `app.js`、`index.html`、`styles.css`、`tests/static-feature-audit-v08.js`、`插件进阶扩展补齐说明-V0.8.25.md`、`设计图/kanboard-v0.8.25-plugin-advanced.png`、`README.md` |

### 本轮新增
- 在 `扩展能力` 弹窗中新增访问控制与自定义路由配置区，覆盖 Application / Project AccessMap、Controller、Action、Role、URL rewriting、CSRF 和 UrlHelper 输出类型。
- 新增 Provider 接入配置，覆盖 Avatar Provider、Custom Group Provider、External Link Provider、External Task Provider 和 Mail Transport。
- 新增 Schema 迁移配置，覆盖驱动文件、版本号、表名、事务回滚和迁移模拟日志。
- 新增 Helper / Events / Overrides 配置，覆盖 helper 注册、自定义事件注册监听、模板 override、formatter override 和 CSP override 风险提示。
- 新增进阶代码预览，分别展示 AccessMap / Route、Provider 注册、Schema 文件和 Plugin Base 扩展片段。
- 扩展 `extensionRiskMessages()`，新增路由、权限、Provider、Schema、Helper、Event、Override 和 CSP 的静态风险检查。
- 将 localStorage key 升级为 `kanboard-static-v0825`，并将项目 JSON 导出版本同步为 `kanboard-static-v0825`。

### 关键判断
- 插件开发能力应该分层表达：V0.8.23 是 Webhook/API/Plugin.php 骨架，V0.8.24 是认证/动作/通知扩展，V0.8.25 是更底层的插件接入点。
- Provider 能力是外部系统集成的抽象，不应该被拆散成多个无联系按钮；统一配置能帮助用户理解 Kanboard 的扩展模型。
- Schema、Helper、Events 和 Overrides 偏开发者视角，但通过“配置 + 代码预览 + 风险提示”可以转化为产品原型可理解的信息结构。
- CSP override 属于高风险能力，因此本轮只做风险可见和代码预览，不把它当作普通功能开关。

### 验证结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 305 项检查。
- 生成 V0.8.25 插件进阶扩展能力截图。

### 下一版本
继续核对 Kanboard 官网剩余能力，重点看 Custom Forms、平台化安装教程以及管理员信息架构是否仍需要进入静态复现；若差距收敛，再进入 V0.9 Figma 高保真原型。

## V0.8.24 - 插件扩展能力补齐
| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-08 |
| 状态 | 已完成 |
| 主题 | 根据 Kanboard Authentication Architecture、Authentication Providers、Automatic Actions 和 Notification Types 文档补齐插件扩展能力 |
| 主要文件 | `app.js`、`index.html`、`styles.css`、`tests/static-feature-audit-v08.js`、`插件扩展能力补齐说明-V0.8.24.md`、`设计图/kanboard-v0.8.24-extension-lab.png`、`README.md` |

### 本轮新增
- 顶部新增 `扩展能力` 入口，位于 `开发者` 与 `运维任务` 之间。
- 新增 `state.extensions.authProviders`，覆盖认证 provider 接口类型、类名、外部 ID 字段、自动创建用户、外部组同步、SessionCheck 和 2FA 后置检查。
- 新增认证流程视图，展示 SessionCheck、Pre、Password、OAuth、Post 和 UserProvider 本地同步顺序。
- 新增已知 provider 列表，区分 Kanboard native provider 与插件 provider，并支持模拟注册自定义 provider。
- 新增认证 provider 代码预览，展示 `AuthenticationManager` 注册和外部用户同步字段。
- 新增 `state.extensions.automaticActions`，覆盖自动动作类名、兼容事件、自定义事件、动作必填参数、事件必备参数、触发条件和执行日志。
- 新增自动动作代码预览，展示 `getCompatibleEvents()`、`getActionRequiredParameters()`、`getEventRequiredParameters()`、`hasRequiredCondition()`、`doAction()`、`register()` 和 `addEvent()`。
- 新增 `state.extensions.notificationTypes`，覆盖通知类型 key、显示名称、处理器 class、适用范围、测试事件、endpoint 和投递日志。
- 新增通知类型代码预览，展示 `NotificationInterface`、`notifyUser()`、`notifyProject()` 和 user/project `setType()` 注册。
- 新增扩展风险检查，覆盖认证类名、OAuth 外部 ID、自动用户同步、SessionCheck、自动动作参数、事件上下文、通知 key、通知处理器和 endpoint。
- 将 localStorage key 升级为 `kanboard-static-v0824`，并将项目 JSON 导出版本同步为 `kanboard-static-v0824`。

### 关键判断
- `插件` 是安装管理，`开发者` 是 Webhook/API/Plugin.php，`扩展能力` 是插件实际改造 Kanboard 核心机制的能力面，三者应分层表达。
- 认证扩展必须呈现执行顺序和用户同步字段，否则用户只会看到 OAuth/LDAP 名词，看不见 AuthenticationManager 的真正工作流。
- 自动动作扩展不能只看事件名，还必须让用户理解动作参数、事件参数、条件判断和 `doAction()` 的闭环。
- 通知扩展需要区分 user 和 project 两个注册入口，因为这会影响最终用户和项目级通知配置。

### 验证结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 287 项检查。
- 生成 V0.8.24 插件扩展能力截图。

### 下一版本
继续检查 Kanboard 官网剩余插件开发与系统能力：授权架构、Avatar providers、Custom Forms、Custom Group Providers、Custom Routes、External Link Providers、External Task Providers、Mail Transport、Plugin Overrides、Schema Migrations、Registering Helpers、Using Events，以及 Debian/Ubuntu/RHEL/Windows 平台化安装教程是否还需要静态复现。

## V0.8.23 - 开发者集成补齐
| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-08 |
| 状态 | 已完成 |
| 主题 | 根据 Kanboard Webhooks、API、Plugin Registration、Plugin Hooks、Plugin Installation 和 Metadata 文档补齐开发者集成入口 |
| 主要文件 | `app.js`、`index.html`、`styles.css`、`tests/static-feature-audit-v08.js`、`开发者集成补齐说明-V0.8.23.md`、`设计图/kanboard-v0.8.23-developer-integrations.png`、`README.md` |

### 本轮新增
- 顶部新增 `开发者` 入口，放在 `部署检查` 与 `运维任务` 之间，表达实例部署后对接外部系统和二次开发的能力层。
- 新增 `state.developer.webhooks`，覆盖 Webhook URL、token、事件、响应预算、私网访问、接收状态和投递日志。
- 新增 Webhook payload 预览，按官网结构展示 `event_name`、`event_data`、请求头和 query token。
- 新增 Webhook 风险提示：开启但缺 URL/token、接收预算超过 1 秒、允许私网访问导致 SSRF 风险。
- 新增 `state.developer.api`，覆盖 JSON-RPC endpoint、Application/User API、认证方式、自定义 Header、procedure、批量请求和调用日志。
- 新增 API procedure 分组索引，覆盖 Application、Project、Board、Task、Subtask、User、Group、Metadata 和 File 等常见 API 域。
- 新增 JSON-RPC 请求预览与模拟响应，支持单个请求和批量请求表达。
- 新增 `state.developer.pluginDev`，覆盖插件目录名、namespace、作者、版本、兼容版本、homepage、schema、metadata、API method、hook、监听事件和骨架生成时间。
- 新增 `Plugin.php` 骨架预览，展示插件目录结构、`Plugin extends Base`、`initialize()`、hook 注册、API method 注册、事件监听和 `getCompatibleVersion()`。
- 将 localStorage key 升级为 `kanboard-static-v0823`，并将项目 JSON 导出版本同步为 `kanboard-static-v0823`。

### 关键判断
- Webhook 是 Kanboard 主动通知外部系统，API 是外部系统主动调用 Kanboard，两者方向不同，界面需要并列但不能混为一个“接口配置”。
- Webhook 同步请求和 1 秒响应要求是使用风险，应该在产品界面前置，而不是只放到说明文档。
- 插件能力不只是“安装插件”，还包括开发入口、目录结构、兼容版本、hooks、metadata 和 API method 注册。
- 本轮仍然是静态复现，不真实发 HTTP 请求、不保存真实凭据、不执行 PHP 插件代码，只做状态、风险、预览和日志模拟。

### 验证结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 264 项检查。
- 生成 V0.8.23 开发者集成截图。

### 下一版本
继续检查 Kanboard 官网剩余能力：Debian/Ubuntu/RHEL/Windows 平台化安装教程、OAuth2/外部认证提供者、自动动作扩展、通知类型扩展、更多插件开发子能力，以及系统级管理员信息架构是否需要继续拆分；Figma 高保真原型继续推迟到完整复现进一步收敛之后。

## V0.8.22 - 安装部署与访问安全补齐

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-07 |
| 状态 | 已完成 |
| 主题 | 根据 Kanboard Installation、Docker、URL Rewriting、Reverse Proxy Authentication 和 Security 文档补齐部署检查 |
| 主要文件 | `app.js`、`index.html`、`styles.css`、`tests/static-feature-audit-v08.js`、`安装部署与访问安全补齐说明-V0.8.22.md`、`设计图/kanboard-v0.8.22-deployment-default.png`、`设计图/kanboard-v0.8.22-docker-healthcheck.png`、`README.md` |

### 本轮新增
- 顶部新增 `部署检查` 入口，与 `系统设置`、`运行环境`、`运维任务` 同级。
- 新增 `state.deployment.install`，覆盖安装方式、版本、安装路径、访问地址、默认密码、data 目录保护、`.htaccess/web.config`、data 可写、DocumentRoot 外安装和 requirements。
- 新增 `state.deployment.docker`，覆盖 Registry、Image Tag、固定版本、data/plugins/ssl Volume、环境变量配置、data 卷 config.php、healthcheck、Compose 数据库方案和 Docker 邮件传输规划。
- 新增 `state.deployment.access`，覆盖 Web Server、URL Rewrite、子目录、data deny、隐藏文件 deny、反向代理、直连阻断、认证 Header 清理、X-Forwarded Header 覆盖、可信代理网段、X-Forwarded-Proto、限流和出站网络限制。
- 新增部署风险面板，覆盖默认密码、data 暴露、Docker latest/nightly、Volume 缺失、healthcheck 缺失、Docker 邮件方式、URL Rewrite、反向代理可信网段、Header 伪造、登录限流和 SSRF 风险。
- 新增部署 Runbook 预览，生成安装参数、URL Rewrite、反向代理加固和 Docker 命令片段。
- 新增模拟 `healthcheck.php`，写入 `200 OK` 或 `503 Service Unavailable` 日志。
- 将 localStorage key 升级为 `kanboard-static-v0822`，并将项目 JSON 导出版本同步为 `kanboard-static-v0822`。

### 关键判断
- 部署检查属于上线前实例级验收，不应放在单个项目配置里。
- `运行环境` 解决 PHP/数据库是否满足运行，`部署检查` 解决安装、暴露、代理和监控是否安全。
- 静态复现不真实启动 Docker 或请求 healthcheck，只表达操作状态、风险反馈和 Runbook 片段。
- 默认密码、data 目录和反向代理 Header 是官网高频强调的上线风险，应在界面上可见。

### 验证结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 240 项检查。
- 生成 V0.8.22 部署检查默认状态截图。
- 生成 V0.8.22 Docker 与 healthcheck 状态截图。

### 下一版本
继续检查 Kanboard 官网剩余能力：Webhook 机制、插件开发相关入口、API procedure 更细入口，以及 Debian/Ubuntu/RHEL/Windows 等平台化安装教程是否需要进入静态复现；Figma 高保真原型继续推迟到完整复现补齐之后。

## V0.8.21 - 运行环境、数据库与升级检查补齐

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-07 |
| 状态 | 已完成 |
| 主题 | 根据 Kanboard Requirements、Configuration、SQLite、Upgrade、Performance 和 PostgreSQL 文档补齐运行环境检查 |
| 主要文件 | `app.js`、`index.html`、`styles.css`、`tests/static-feature-audit-v08.js`、`运行环境数据库升级补齐说明-V0.8.21.md`、`设计图/kanboard-v0.8.21-runtime-default.png`、`设计图/kanboard-v0.8.21-runtime-upgrade-readiness.png`、`README.md` |

### 本轮新增
- 顶部新增 `运行环境` 入口，与 `系统设置`、`运维任务` 同级。
- 新增 `state.runtime.database`，记录数据库驱动、SQLite 文件、远程数据库连接、WAL、自动迁移、Schema 版本、备份和优化时间。
- 新增 `state.runtime.environment`，记录 PHP 版本、Web Server、安装方式、部署场景、data/缓存目录权限、FILES_DIR、缓存驱动、OpCode、URL Rewrite 和 DEBUG。
- 新增 `state.runtime.php.extensions`，按当前数据库驱动动态检查核心 PHP 扩展和对应 PDO 扩展。
- 新增 `state.runtime.upgrade`，覆盖 ChangeLog、备份验证、停止 Worker、维护模式、清理会话、插件兼容检查。
- 新增运行环境风险提示：PHP 版本、缺失扩展、SQLite 部署场景、目录权限、缓存权限、OpCode、DEBUG、迁移前备份、升级前 Worker/维护模式。
- 新增 `config.php` 预览，映射 `DB_DRIVER`、`DB_RUN_MIGRATIONS`、`DB_WAL_MODE`、`FILES_DIR`、`CACHE_DRIVER`、`ENABLE_URL_REWRITE`、`DEBUG` 等配置。
- 支持模拟数据库备份、模拟运行迁移、模拟优化数据库，并写入运行环境操作日志。
- 将 localStorage key 升级为 `kanboard-static-v0821`，并将项目 JSON 导出版本同步为 `kanboard-static-v0821`。

### 关键判断
- 运行环境属于实例级管理员能力，不应放入单个项目设置。
- 静态复现只表达操作状态和风险反馈，不真实操作数据库。
- 数据库驱动变化会影响必需 PDO 扩展，所以扩展检查必须动态跟随。
- 升级流程的核心不是一个“升级按钮”，而是升级前检查是否完整可见。

### 验证结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 221 项检查。
- 生成 V0.8.21 运行环境默认状态截图。
- 生成 V0.8.21 PostgreSQL 与升级准备状态截图。

### 下一版本
继续检查 Kanboard 官网剩余系统级差距：安装教程、Docker 部署、反向代理部署提示、URL Rewrite 细节、Webhook/API 更细入口和插件开发相关能力；Figma 高保真原型继续推迟到完整复现补齐之后。

## V0.8.20 - 运维任务与后台执行补齐

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-07 |
| 状态 | 已完成 |
| 主题 | 根据 Kanboard CLI、Cronjob、Background Worker、Email 和配置文件文档补齐运维执行层 |
| 主要文件 | `app.js`、`index.html`、`styles.css`、`tests/static-feature-audit-v08.js`、`运维任务与后台执行补齐说明-V0.8.20.md`、`设计图/kanboard-v0.8.20-operations-default.png`、`设计图/kanboard-v0.8.20-operations-run-log.png`、`README.md` |

### 本轮新增
- 顶部新增 `运维任务` 入口。
- 新增 `state.operations.cron`，记录 Cronjob 执行方式、Cron 表达式、Windows 任务名、Cron URL、最后运行时间和状态。
- 新增 `state.operations.worker`，记录 Worker 是否启用、队列驱动、Supervisor、处理数量和失败数量。
- 新增 `state.operations.mail`，记录邮件传输方式、From、SMTP 主机、端口、加密方式、SMTP 用户、应用 URL、测试收件人和测试邮件时间。
- 新增 `state.operations.cli`，记录 CLI 当前选择命令和运行日志。
- 新增后台任务队列：项目每日统计、逾期任务通知、每日任务触发器、插件升级检查。
- 支持模拟运行 Cronjob，一次性完成每日后台任务并写入 CLI 日志。
- 支持模拟 Worker 处理一个队列任务，并更新处理计数。
- 支持模拟发送测试邮件，并记录最后测试时间。
- 支持选择 CLI 命令并生成命令预览。
- 支持模拟执行 CLI 命令，记录运行结果和时间。
- 将 localStorage key 升级为 `kanboard-static-v0820`，并将项目 JSON 导出版本同步为 `kanboard-static-v0820`。

### 关键判断
- 运维能力要表达“配置、调度、执行、日志”闭环，不只是罗列 CLI 命令。
- Cronjob 是 Kanboard 每日统计、逾期通知和自动动作的重要前提，应成为系统级检查项。
- Background Worker 文档明确该能力不再维护，因此静态复现中保留“可选队列”定位，不把它表现成默认推荐路径。
- 邮件配置必须和测试收件人、应用 URL 放在一起，否则无法解释通知邮件链路。

### 验证结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 201 项检查。
- 生成 V0.8.20 运维任务默认状态截图。
- 生成 V0.8.20 Cronjob、Worker、邮件和 CLI 运行日志截图。

### 下一版本
继续检查 Kanboard 剩余系统级差距：数据库配置、SQLite/MySQL/PostgreSQL、性能优化、附件存储路径、升级流程、安装环境检查等；Figma 高保真原型继续推迟到完整复现补齐之后。

## V0.8.19 - 系统认证与安全配置补齐

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-06 |
| 状态 | 已完成 |
| 主题 | 根据 Kanboard API 认证、LDAP、反向代理认证、配置文件和安全文档补齐系统配置 |
| 主要文件 | `app.js`、`index.html`、`styles.css`、`tests/static-feature-audit-v08.js`、`系统认证与安全配置补齐说明-V0.8.19.md`、`设计图/kanboard-v0.8.19-system-settings-default.png`、`设计图/kanboard-v0.8.19-auth-risk-config.png`、`设计图/kanboard-v0.8.19-config-preview.png`、`README.md` |

### 本轮新增
- 顶部新增 `系统设置` 入口。
- 新增 `state.system.api`，记录 API endpoint、应用 API 用户、API token、自定义认证 Header 和 2FA 用户 API Key 要求。
- 新增 `state.system.ldap`，记录 LDAP 开关、服务器、bind type、User Base DN、User Filter、自动创建用户、用户组同步、Group Base DN 和 Group Filter。
- 新增 `state.system.reverseProxy`，记录反向代理认证开关、可信代理网络、用户/邮箱/姓名 Header、默认管理员、默认邮箱域名和 Header 清理状态。
- 新增 `state.system.security`，记录隐藏登录表单、禁用登出、Markdown HTML 转义、暴力破解阈值、锁定时长、外链/Webhook 私网访问开关。
- 新增系统设置摘要卡：API 认证方式、LDAP 状态、反向代理状态、安全提示数量。
- 新增风险提示：反向代理缺少可信网络、未清理认证 Header、隐藏登录表单但未启用外部认证、允许私网访问、2FA 用户未要求 API Key、LDAP 缺少 User Base DN。
- 新增 `config.php` 静态预览，自动生成与当前开关对应的配置片段。
- 将 localStorage key 升级为 `kanboard-static-v0819`，并将项目 JSON 导出版本同步为 `kanboard-static-v0819`。

### 关键判断
- API、LDAP、反向代理和安全选项是实例级配置，不应混入项目设置或单个用户资料。
- 反向代理认证必须和可信代理网络、Header 清理一起表达，否则会遗漏最关键的安全前置条件。
- config.php 预览能帮助产品设计从界面状态连接到真实部署配置，是这一轮的核心学习价值。

### 验证结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 188 项检查。
- 生成 V0.8.19 系统设置默认配置截图。
- 生成 V0.8.19 外部认证风险状态截图。
- 生成 V0.8.19 config.php 预览截图。

### 下一版本
继续检查 Kanboard 剩余系统级差距：管理员 CLI、Cronjob、后台任务、邮件配置、数据库/性能配置等；Figma 高保真原型继续推迟到完整复现补齐之后。

## V0.8.18 - 全局用户与用户组管理补齐

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-06 |
| 状态 | 已完成 |
| 主题 | 根据 Kanboard Users and Groups、User API、Group API 和 Group Member API 补齐实例级用户与组管理 |
| 主要文件 | `app.js`、`index.html`、`styles.css`、`tests/static-feature-audit-v08.js`、`全局用户与用户组管理补齐说明-V0.8.18.md`、`设计图/kanboard-v0.8.18-user-directory.png`、`设计图/kanboard-v0.8.18-group-membership.png`、`README.md` |

### 本轮新增
- 顶部新增 `用户管理` 入口。
- 新增全局身份目录 `state.identity.users`，区分全局用户与项目内成员权限。
- 新增全局用户组 `state.identity.groups`，支持成员关系。
- 默认生成管理员、PM、设计、研发、测试、运营等用户，以及产品研发测试小组、运营观察组。
- 支持新增本地用户，并校验本地用户密码至少 6 位。
- 支持新增远程用户，记录 LDAP、反向代理、OAuth2 等外部身份来源。
- 支持更新应用角色：管理员、经理、用户。
- 支持启用/停用用户，阻止停用、删除或降级最后一个启用状态管理员。
- 支持开启/关闭 2FA，并提示 Kanboard 1.2.8 起 API 访问需使用 API Key。
- 支持新增用户组、加入组、移出组、删除组。
- 删除用户时同步移出用户组，并把该用户负责的任务改为未分配。
- 任务负责人筛选和输入候选项纳入启用状态的全局用户。
- 将 localStorage key 升级为 `kanboard-static-v0818`，并将项目 JSON 导出版本同步为 `kanboard-static-v0818`。

### 关键判断
- 用户和用户组是实例级身份基础设施，不应该只放在项目设置里。
- 项目权限应该引用全局用户/组，再在项目内赋予项目级角色。
- 本轮只做静态复现，不做真实登录、密码保存、LDAP/OAuth2 连接、TOTP 二维码或后端鉴权。

### 验证结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 175 项检查。
- 生成 V0.8.18 全局用户目录截图。
- 生成 V0.8.18 用户组成员关系截图。

### 下一版本
继续检查 Kanboard 剩余系统级差距：外部认证配置、管理员 CLI、后台任务、系统配置与安全设置等；Figma 高保真原型继续推迟到完整复现补齐之后。

## V0.8.17 - 插件管理补齐

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-06 |
| 状态 | 已完成 |
| 主题 | 根据 Kanboard 插件安装、插件目录、插件注册和 CLI 插件命令补齐静态插件管理入口 |
| 主要文件 | `app.js`、`index.html`、`styles.css`、`tests/static-feature-audit-v08.js`、`插件管理补齐说明-V0.8.17.md`、`设计图/kanboard-v0.8.17-plugin-installer-disabled.png`、`设计图/kanboard-v0.8.17-plugin-installer-enabled.png`、`README.md` |

### 本轮新增
- 顶部新增 `插件` 入口，打开插件管理弹窗。
- 新增插件安装器状态卡，展示安装器是否可用、已安装、可安装和可更新数量。
- 新增安装器配置：`允许 Web UI 安装插件`、`插件目录可写`、`PHP Zip 扩展可用` 和插件目录 URL。
- 默认关闭 Web UI 安装器，安装按钮不可用，贴近 Kanboard v1.2.8 之后的安全默认值。
- 满足安装器、目录权限、Zip 扩展三个条件后，允许模拟安装插件。
- 已安装插件列表支持升级和卸载，插件目录列表支持安装未安装插件。
- 将插件状态纳入 `localStorage`，并将 key 升级为 `kanboard-static-v0817`。
- 将项目 JSON 导出版本同步为 `kanboard-static-v0817`，保证导入导出版本链路一致。

### 关键判断
- 插件管理是管理员/系统级能力，不能只复现“安装”按钮，还要表达安全前置条件和默认关闭状态。
- 官网说明插件提交没有代码审查流程，实例所有者需要验证插件来源，因此页面上保留安全提示是必要信息，而不是装饰文案。
- 本轮只做静态复现，不下载真实 Zip 包、不写入真实插件目录、不执行 PHP 插件代码。

### 验证结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 162 项检查。
- 生成 V0.8.17 插件安装器默认关闭状态截图。
- 生成 V0.8.17 插件安装、升级后状态截图。

### 下一版本
继续检查 Kanboard 剩余系统级差距：全局用户/组管理、外部认证、管理员 CLI、后台任务和系统配置等；Figma 高保真原型继续推迟到完整复现补齐之后。

## V0.8.16 - 导入导出补齐

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-06 |
| 状态 | 已完成 |
| 主题 | 根据 Kanboard CLI CSV 导出和 API 迁移能力补齐静态导入导出入口 |
| 主要文件 | `app.js`、`index.html`、`styles.css`、`tests/static-feature-audit-v08.js`、`导入导出补齐说明-V0.8.16.md`、`设计图/kanboard-v0.8.16-export-preview.png`、`设计图/kanboard-v0.8.16-import-preview.png`、`README.md` |

### 本轮新增
- 顶部新增 `导入导出` 入口。
- 新增导出概览：当前项目、任务数、子任务数、项目文件数。
- 新增 `任务 CSV` 预览，覆盖标题、列、泳道、负责人、分类、优先级、截止日、状态、工时和标签。
- 新增 `子任务 CSV` 预览，覆盖任务 ID、任务标题、子任务 ID、子任务标题和完成状态。
- 新增 `项目 JSON 备份`，导出当前项目结构并标记 `exportVersion: kanboard-static-v0816`。
- 新增 JSON 粘贴导入预览，显示项目名称、列数、泳道数和任务数。
- 新增 `导入为新项目`，导入时重新生成项目、列、泳道、任务、子任务、附件和自动化 ID。
- 新增通用 disabled 按钮样式，避免禁用按钮仍表现为可点击。
- 将 localStorage key 升级为 `kanboard-static-v0816`，确保直接打开首页时加载导入导出入口。

### 关键判断
- 官方 CSV 导出更偏管理员/运维侧能力；静态页面先复现格式预览、复制和迁移路径。
- JSON 导入不是 Kanboard 官网原生 UI 的完整复现，而是当前静态原型的可恢复/可迁移能力，服务实战项目演示。
- 导入项目必须重映射 ID，否则复制项目会与当前项目发生列、泳道、任务引用冲突。

### 验证结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 152 项检查。
- 生成 V0.8.16 任务 CSV 导出预览截图。
- 生成 V0.8.16 项目 JSON 导入预览截图。

### 下一版本
继续检查 Kanboard 剩余系统级差距：插件配置、全局用户/组管理、外部认证、管理员 CLI 能力等；当前静态复现也已经可以进入 V0.9 Figma 高保真原型。

## V0.8.15 - 公共访问与订阅补齐

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-04 |
| 状态 | 已完成 |
| 主题 | 根据 Kanboard iCalendar、RSS/Atom 和公共访问机制补齐项目订阅入口 |
| 主要文件 | `app.js`、`index.html`、`styles.css`、`tests/static-feature-audit-v08.js`、`公共访问与订阅补齐说明-V0.8.15.md`、`设计图/kanboard-v0.8.15-public-access-settings.png`、`设计图/kanboard-v0.8.15-subscription-links.png`、`README.md` |

### 本轮新增
- 项目设置新增 `开启公共访问与订阅` 开关。
- 项目设置新增 `publicAccessEnabled` 和 `publicToken`，用于表达项目级公共订阅状态。
- 顶部新增 `订阅` 入口。
- 新增 iCalendar 静态订阅链接，用于表达项目任务日期订阅。
- 新增 RSS/Atom 静态订阅链接，用于表达项目活动流订阅。
- 公共访问关闭时订阅链接不可用，复制按钮禁用。
- 复制链接采用“选中链接 + 状态反馈”的静态交互，不调用真实剪贴板权限。
- Overview 的访问权限摘要新增 `公共订阅` 状态。
- 将 localStorage key 升级为 `kanboard-static-v0815`，确保直接打开首页时加载公共访问与订阅结构。

### 关键判断
- iCalendar 解决任务日期同步到外部日历的问题，RSS/Atom 解决项目活动同步到外部信息流的问题。
- 公共访问必须是订阅能力的前置条件，不能直接暴露订阅链接。
- 本轮仍不实现真实公共 URL、真实 token 校验和真实订阅内容生成，只复现原型层的信息架构和交互状态。

### 验证结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 140 项检查。
- 生成 V0.8.15 公共访问设置截图。
- 生成 V0.8.15 iCalendar 与 RSS/Atom 订阅截图。

### 下一版本
继续检查 Kanboard 剩余系统级差距：导入导出、插件配置、全局用户/组管理、外部认证等；也可以开始 V0.9 Figma 高保真原型。

## V0.8.14 - 项目文件与权限补齐

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-04 |
| 状态 | 已完成 |
| 主题 | 根据 Kanboard 官方 Project Overview、Project File API 和 Project Permission API 补齐项目文件库、访问范围和用户组权限 |
| 主要文件 | `app.js`、`index.html`、`styles.css`、`tests/static-feature-audit-v08.js`、`项目文件与权限补齐说明-V0.8.14.md`、`设计图/kanboard-v0.8.14-project-files-permissions.png`、`设计图/kanboard-v0.8.14-overview-permissions.png`、`README.md` |

### 本轮新增
- 新增项目级 `files` 数据结构，区分项目文件与任务附件。
- 项目设置新增 `项目文件` 区块，可添加文件名/链接、类型/大小、负责人，并支持删除。
- 默认 PM 工作流项目生成需求调研、PRD 和上线检查清单等项目资料。
- 个人学习项目生成学习路线和作品集素材清单。
- 项目属性新增 `访问范围`：私有项目、团队可见、公开只读。
- 项目设置新增 `用户组权限`，支持用户组新增、删除和管理员/成员/访客角色切换。
- Overview 新增项目文件数量、成员/用户组数量、项目文件列表和访问权限摘要。
- 项目活动流纳入项目文件记录。
- 将 localStorage key 升级为 `kanboard-static-v0814`，确保直接打开首页时加载文件与权限结构。

### 关键判断
- 项目文件用于项目级沉淀，任务附件用于单个任务上下文，二者不应混在同一个模型里。
- 静态原型阶段只表达权限配置路径和角色关系，不模拟真实登录、鉴权和接口拦截。
- Overview 是项目状态入口，应该能看到项目文件、成员和权限摘要，不能只依赖项目设置弹窗。

### 验证结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 129 项检查。
- 生成 V0.8.14 项目文件与用户组权限设置截图。
- 生成 V0.8.14 Overview 文件与访问权限摘要截图。

### 下一版本
继续检查 Kanboard 剩余系统级差距：导入导出、用户/组全局管理、插件配置、订阅、外部认证等；静态复现层面已经具备进入 V0.9 Figma 高保真的基础。

## V0.8.13 - 活动附件快捷键补齐

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-04 |
| 状态 | 已完成 |
| 主题 | 根据 Kanboard 官网与官方文档补齐项目活动流、任务附件和快捷键基础能力 |
| 主要文件 | `app.js`、`index.html`、`styles.css`、`tests/static-feature-audit-v08.js`、`活动附件快捷键补齐说明-V0.8.13.md`、`设计图/kanboard-v0.8.13-task-attachments.png`、`设计图/kanboard-v0.8.13-activity-stream.png`、`README.md` |

### 本轮新增
- 新增顶部 `活动` 入口，汇总当前项目的任务创建、任务更新、评论、耗时、附件和通知记录。
- 新增任务详情 `附件` 区块，可记录文件名或链接、类型和大小等静态元信息。
- 任务卡片增加附件数量提示，附件新增和删除会写入任务活动记录。
- 新增顶部 `快捷键` 入口，展示当前静态原型支持的键盘操作。
- 快捷键调整为更接近 Kanboard 官方习惯：`v b`、`v c`、`v l`、`v g`、`v o` 切换项目视图。
- 新增 `f` 聚焦搜索、`r` 重置搜索、`n` 新建任务、`s` 展开/折叠任务卡、`c` 紧凑/展开看板。
- 将 localStorage key 升级为 `kanboard-static-v0813`，确保直接打开首页时加载新的活动、附件和快捷键结构。

### 关键判断
- 活动流用于回答“项目发生了什么”，是项目复盘、协作追踪和任务上下文的重要入口。
- 附件用于承载 PRD、评审材料、测试截图、运营素材等交付物线索，适合 PM 完整工作流。
- 快捷键不能随意设计成单键切换视图，应尽量贴近 Kanboard 官方文档里的 `v + 视图键` 操作模型。
- 真实文件上传、权限、导入导出、插件和 API 仍属于后端/系统级能力，本轮只做静态复现层面的数据和交互。

### 验证结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 121 项检查。
- 生成 V0.8.13 任务附件截图。
- 生成 V0.8.13 项目活动流截图。

### 下一版本
继续根据 Kanboard 官网检查剩余差距：项目文件库、导入导出、权限限制、用户与组、通知订阅、插件配置等系统能力；静态复现可先进入 V0.9 Figma 高保真原型。

## V0.8.12 - 视图与菜单补齐

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-03 |
| 状态 | 已完成 |
| 主题 | 根据 Kanboard 官方项目视图补齐 Calendar、Gantt，并增强 Board 菜单操作 |
| 主要文件 | `app.js`、`index.html`、`styles.css`、`tests/static-feature-audit-v08.js`、`视图与菜单补齐说明-V0.8.12.md`、`设计图/kanboard-v0.8.12-calendar-view.png`、`设计图/kanboard-v0.8.12-gantt-view.png`、`README.md` |

### 本轮新增
- 视图切换从 `看板 / 列表 / 概览` 扩展为 `看板 / 日历 / 列表 / 甘特 / 概览`。
- 新增 Calendar 日历视图，按截止日、计划完成日、实际完成日或创建日展示任务。
- 新增 Gantt 甘特视图，按计划开始、计划完成、截止日生成任务排期条。
- Calendar 和 Gantt 都复用顶部搜索、负责人、分类、泳道、自定义筛选。
- 列三点按钮改为下拉菜单，支持新增任务、编辑列、隐藏此列。
- 任务卡片新增下拉菜单，支持编辑任务、复制任务、关闭 / 重新打开。
- 将 localStorage key 升级为 `kanboard-static-v0812`，确保直接打开首页时加载新的视图和菜单。

### 关键判断
- Kanboard 官方项目视图包括 Board、Calendar、List、Gantt；本轮补齐 Calendar 和 Gantt。
- Overview 是我们为静态复现补充的项目分析入口，不替代官方 Calendar / Gantt。
- Board 菜单应承载常用操作，不能只把三点按钮做成“编辑”的快捷方式。

### 验证结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 112 项检查。
- 生成 V0.8.12 Calendar 日历视图截图。
- 生成 V0.8.12 Gantt 甘特视图截图。

### 下一版本
继续根据 Kanboard 官网检查剩余差距：项目配置细节、快捷键、活动流、附件/文件、权限限制、导入导出和插件相关能力。

## V0.8.11 - 泳道建模逻辑修正

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-03 |
| 状态 | 已完成 |
| 主题 | 根据 Kanboard 官方泳道模型修正主 PM 项目默认泳道设计 |
| 主要文件 | `app.js`、`index.html`、`tests/static-feature-audit-v08.js`、`泳道建模逻辑修正说明-V0.8.11.md`、`设计图/kanboard-v0.8.11-swimlane-model-fix.png`、`README.md` |

### 本轮修正
- 主项目 `产品开发全流程项目` 默认从 4 条泳道改为 1 条泳道：`产品开发主流程`。
- 保留 11 个产品开发状态列，继续表达从机会/需求池到复盘迭代的完整流转。
- 25 张 PM 工作流任务全部放在同一条主流程泳道中。
- 原先需求、产品、研发、测试、运营等工作差异继续通过卡片分类、标签和负责人表达。
- 保留新增、编辑、禁用泳道能力，但默认示例不再滥用泳道。
- 将 localStorage key 升级为 `kanboard-static-v0811`，确保直接打开首页时加载新的泳道模型。

### 关键判断
- Kanboard 的列表示任务状态，泳道表示横向分组；多个泳道会共享同一套列。
- 单一产品开发项目默认应该是一条主流程泳道，不应该把需求、产品、研发、运营这些工作类型拆成 4 条泳道。
- 只有多个版本、多个产品线、多个团队等场景，才适合作为多泳道默认结构。
- 个人学习项目保留多泳道，因为它的泳道表达能力模块，符合横向分组语义。

### 验证结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 105 项检查。
- 生成 V0.8.11 泳道建模逻辑修正截图。

### 下一版本
V0.9：基于更接近 Kanboard 官方模型的静态页面进入 Figma 高保真原型。

## V0.8.10 - 弹窗关闭逻辑修复

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-03 |
| 状态 | 已完成 |
| 主题 | 修复必填表单弹窗点击关闭或取消时被 required 校验拦截的问题 |
| 主要文件 | `index.html`、`app.js`、`tests/static-feature-audit-v08.js`、`弹窗关闭逻辑修复说明-V0.8.10.md`、`设计图/kanboard-v0.8.10-dialog-close-fix.png`、`README.md` |

### 本轮修复
- 所有 `value="cancel"` 的关闭、取消按钮增加 `formnovalidate`。
- 修复新建项目、新增列、新增泳道、新增任务等弹窗在空必填字段时无法关闭的问题。
- 保留保存按钮的 required 校验，避免创建空名称数据。
- 将 localStorage key 升级为 `kanboard-static-v0810`，确保直接打开首页时加载本轮修复版本。

### 关键判断
- 关闭和取消是退出动作，不应该触发保存表单的必填校验。
- 保存动作仍然必须校验 required，这是数据完整性约束。
- 这个问题属于产品逻辑和前端表单机制的交叉 bug，必须用自动化测试覆盖，避免后续新增弹窗时复发。

### 验证结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 104 项检查。
- 生成 V0.8.10 弹窗关闭逻辑修复截图。

### 下一版本
V0.9：进入 Figma 高保真原型前，继续检查静态页面中是否还有类似“取消动作误触发表单校验”的基础交互问题。

## V0.8.9 - 列显示面板优化

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-03 |
| 状态 | 已完成 |
| 主题 | 将一排隐藏列按钮收敛为可展开的列显示选择面板 |
| 主要文件 | `app.js`、`index.html`、`styles.css`、`tests/static-feature-audit-v08.js`、`列显示面板优化说明-V0.8.9.md`、`设计图/kanboard-v0.8.9-column-picker.png`、`README.md` |

### 本轮新增
- 将工具栏里的多个“隐藏/显示列”按钮收敛为一个 `列显示` 折叠控件。
- 摘要展示当前可见列数量，例如 `11/11 已显示`。
- 展开后用复选列表选择每个流程列是否显示。
- 面板展开后连续勾选不会自动收起，适合批量调整。
- 增加 `全部显示` 快捷操作。
- 增加 `隐藏无任务列` 快捷操作，适合处理新增但暂时没有任务的流程列。
- 隐藏列数据会清理无效列 id，并至少保留 1 个可见列。
- 将 localStorage key 升级为 `kanboard-static-v089`，确保直接打开首页时加载新的列显示控件。

### 关键判断
- 列隐藏能力是辅助控制，不应该在常态工具栏中占据大量空间。
- 流程列数量变多后，列表选择比一排按钮更适合承载多选项。
- 这次优化的核心不是增加能力，而是建立更清晰的信息层级。

### 验证结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 100 项检查。
- 生成 V0.8.9 列显示面板优化截图。

### 下一版本
V0.9：基于更完整、更收敛的静态页面进入 Figma 高保真原型。

## V0.8.8 - 项目周期时间系统

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-03 |
| 状态 | 已完成 |
| 主题 | 在 Kanboard 复现版中加入项目级周期、卡片计划实际日期、阶段周期分析和风险提示 |
| 主要文件 | `app.js`、`index.html`、`styles.css`、`tests/static-feature-audit-v08.js`、`项目周期时间系统说明-V0.8.8.md`、`设计图/kanboard-v0.8.8-project-cycle-system.png`、`README.md` |

### 本轮新增
- 新增项目级时间设置：计划启动、计划上线、实际启动、实际上线。
- 新增卡片级计划与实际日期：计划开始、计划完成、实际开始、实际完成。
- 项目分析弹窗新增 `项目周期` 总览，展示计划周期、已运行、当前阶段、周期风险、超期任务和计划上线。
- 新增阶段周期分析，按流程列聚合计划天数、实际天数、任务数量、超期数量和阶段偏差。
- 新增周期风险面板，提示上线延期、阶段超期和任务超期。
- 默认 PM 项目补充完整演示排期，让产品提需、文档、开发、测试、运营、上线、反馈维护形成可分析的时间链路。
- 将 localStorage key 升级为 `kanboard-static-v088`，确保直接打开首页时加载新的周期演示数据。

### 关键判断
- 本轮没有改成重型甘特图，而是在 Kanboard 看板模型上叠加周期分析。
- 项目周期服务于 PM 判断：当前是否延期、哪个阶段阻塞、哪些任务应该优先沟通。
- 对新手学习来说，时间系统能帮助理解产品开发不是只有“任务完成”，还包括计划、排期、延期、风险和复盘。

### 验证结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 97 项检查。
- 生成 V0.8.8 项目周期时间系统截图。

### 下一版本
V0.9：基于更完整的 Kanboard 复现、PM 工作流和项目周期分析，进入 Figma 高保真原型。

## V0.8.7 - 泳道空列精简与流程状态升级

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-02 |
| 状态 | 已完成 |
| 主题 | 增加隐藏空列显示优化，并升级 PM 工作流和个人学习项目流程状态 |
| 主要文件 | `app.js`、`index.html`、`styles.css`、`tests/static-feature-audit-v08.js`、`泳道空列精简与流程状态升级说明-V0.8.7.md`、`设计图/kanboard-v0.8.7-lane-compact-workflow.png`、`README.md` |

### 本轮新增
- 新增 `隐藏空列` 显示开关，默认打开，减少泳道内重复空列。
- 隐藏空列只影响当前泳道内没有任务的列，不改变底层流程数据。
- 全局完全空的新列仍然显示，避免用户新增列后找不到它。
- 主 PM 项目流程从 8 列升级为 11 列：机会/需求池、需求澄清、方案设计、PRD 原型、评审确认、研发排期、研发实现、测试验收、上线发布、运营反馈、复盘迭代。
- 主 PM 项目从 24 张任务卡扩展为 25 张任务卡，补充方案取舍任务。
- 个人学习计划项目升级为 6 个流程状态、7 条能力泳道、19 张学习任务卡。
- 将 localStorage key 升级为 `kanboard-static-v087`，确保直接打开首页时加载新的默认状态。

### 验证结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 89 项检查。
- 生成 V0.8.7 泳道空列精简与流程状态升级截图。

### 下一版本
V0.9：基于更完整的流程状态和更清爽的泳道展示，进入 Figma 高保真原型。

## V0.8.6 - 固定侧栏与右侧滚动修复

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-02 |
| 状态 | 已完成 |
| 主题 | 修复桌面端整页滚动问题，保证左侧导航固定、右侧工作区独立滚动 |
| 主要文件 | `styles.css`、`index.html`、`tests/static-feature-audit-v08.js`、`固定侧栏滚动修复说明-V0.8.6.md`、`设计图/kanboard-v0.8.6-fixed-sidebar-scroll.png`、`README.md` |

### 本轮新增
- 桌面端将页面锁定为 `100vh` app shell，避免浏览器整页滚动带走左侧导航。
- 左侧 `.sidebar` 固定在视口左侧，并为项目列表保留独立滚动能力。
- 右侧 `.workspace` 成为独立纵向滚动容器。
- 修复 `.board-shell` 在 flex 布局中被压缩的问题，让看板内容能撑开右侧滚动高度。
- 移动端仍保留自然页面滚动。
- 静态验收脚本新增 3 项布局断言。

### 验证结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 87 项检查。
- 生成 V0.8.6 固定侧栏与右侧滚动截图。

### 下一版本
V0.9：进入 Figma 高保真原型前，继续以桌面后台工具布局作为视觉与交互基准。

## V0.8.5 - 产品经理学习路线补齐

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-02 |
| 状态 | 已完成 |
| 主题 | 将个人学习项目改造成从 0 开始成为优秀产品经理的成长路线 |
| 主要文件 | `app.js`、`index.html`、`tests/static-feature-audit-v08.js`、`个人产品经理学习路线说明-V0.8.5.md`、`设计图/kanboard-v0.8.5-pm-learning-roadmap.png`、`README.md` |

### 本轮新增
- 将 `个人学习计划项目` 的内容从英语学习改为产品经理成长路线。
- 保留待学、学习中、已学完三列，让个人学习项目仍然轻量。
- 学习项目泳道从 1 条扩展为 4 条：产品认知、用户与需求、方案与表达、协作与成长。
- 学习项目任务从 3 张扩展为 12 张，覆盖 PM 入门、用户研究、需求池、PRD、原型、指标、评审、验收、复盘和作品集。
- 将 localStorage key 升级为 `kanboard-static-v085`，确保首页加载新的学习路线默认数据。
- 更新静态验收脚本，增加学习项目列、泳道、任务数和 PRD 练习断言。

### 关键判断
- 这个项目不是偏离 Kanboard 复现，而是把 Kanboard 的多项目能力用于真实学习路线。
- 产品经理学习应该拆成可推进的任务，而不是只列课程清单。
- 将学习任务绑定到当前 Kanboard 实战项目，可以让后续 PRD、Figma 高保真和作品集表达互相衔接。

### 验证结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 84 项检查。
- 生成 V0.8.5 产品经理学习路线截图。

### 下一版本
V0.9：基于产品开发全流程项目和个人产品经理学习项目，进入 Figma 高保真原型。

## V0.8.4 - 多项目示例补齐

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-02 |
| 状态 | 已完成 |
| 主题 | 在默认首页补齐第二个示例项目，展示 Kanboard 的多项目管理能力 |
| 主要文件 | `app.js`、`index.html`、`tests/static-feature-audit-v08.js`、`多项目示例说明-V0.8.4.md`、`设计图/kanboard-v0.8.4-multi-project-demo.png`、`README.md` |

### 本轮新增
- 默认演示数据从 1 个项目扩展为 2 个项目。
- 保留 `产品开发全流程项目` 为默认 active 项目，继续展示 PM 全流程主场景。
- 新增 `个人学习计划项目`，使用待学、学习中、已学完三列和英语学习相关示例任务。
- 将 localStorage key 升级为 `kanboard-static-v084`，确保直接打开首页时加载新的默认数据。
- 同步 `index.html` 标题、侧栏版本文案和自动化弹窗版本文案。
- 更新静态验收脚本，增加默认双项目和个人学习项目断言。

### 关键判断
- 只展示一个复杂 PM 项目，容易让页面显得像单一 demo；补一个轻量项目能更好体现 Kanboard 的多项目管理属性。
- 第二项目不应该替代主项目，而应该作为侧栏中的可切换项目，帮助用户理解工具可以同时服务不同场景。
- 复杂团队流程和个人学习计划形成对照，有利于后续 Figma 高保真展示“模板化创建”和“场景迁移”。

### 验证结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 80 项检查。
- 生成 V0.8.4 多项目示例截图。

### 下一版本
V0.9：基于 V0.8.4 的双项目默认状态进入 Figma 高保真原型，重点绘制复杂 PM 项目和轻量学习项目的场景差异。

## V0.8.3 - PM 工作流看板补齐

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-02 |
| 状态 | 已完成 |
| 主题 | 用默认示例项目复现完整产品开发流程 |
| 主要文件 | `app.js`、`tests/static-feature-audit-v08.js`、`PM工作流看板说明-V0.8.3.md`、`设计图/kanboard-v0.8.3-pm-workflow.png`、`README.md` |

### 本轮新增
- 默认项目从“产品优化项目”调整为“产品开发全流程项目”。
- 默认列从 4 列扩展为 8 列：需求池、需求澄清、产品方案、评审排期、研发实现、测试验收、上线运营、反馈维护。
- 默认泳道从 2 条扩展为 4 条：需求与用户、产品方案、研发测试、运营反馈。
- 默认任务卡从 3 张扩展为 24 张，覆盖提需、用户反馈、竞品、PRD、原型、埋点、评审、排期、开发、联调、测试、上线、运营、复盘和维护。
- 顶部“进行中”指标口径适配 PM 活跃阶段。
- 更新静态验收脚本的默认项目断言。
- 同步 `index.html` 首页标题、侧栏版本文案和 localStorage key，确保直接打开主页时加载 V0.8.3 默认看板。

### 关键判断
- 看板列用于表达产品开发流程阶段，泳道用于表达跨角色协作工作流。
- 默认示例内容应该能教用户如何拆项目，而不只是证明页面有卡片。
- “反馈维护”不是项目终点，而是下一轮需求池的输入。
- 这轮不改变已有功能，只增强产品场景表达。

### 验证结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 79 项检查。
- 生成 V0.8.3 PM 工作流看板截图。

### 下一版本
V0.9：基于完整 PM 工作流场景进入 Figma 高保真原型，重点表现“空白项目路径”和“产品开发全流程模板路径”的差异。

## V0.8.2 - Open-source Command Desk 界面重设计

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-02 |
| 状态 | 已完成 |
| 主题 | 使用前端设计类 Skill 重塑 Kanboard 静态页视觉系统 |
| 主要文件 | `styles.css`、`界面重设计说明-V0.8.2.md`、`设计图/kanboard-v0.8.2-command-desk-redesign.png`、`README.md` |

### 使用 Skill
- `frontend-design`：定义 Open-source Command Desk 审美方向和 DFII 判断。
- `baseline-ui`：收紧圆角、阴影、动效和模板化组件痕迹。
- `kpi-dashboard-design`：优化顶部指标为可扫读状态条。
- `design-spells`：增加克制记忆点，如品牌短竖条、列状态色、任务状态点。
- `fixing-accessibility`：复核焦点态、移动端溢出和颜色对比。

### 本轮新增
- 侧栏改为深色项目导航，当前项目使用白色选中块和主色左边线。
- 顶部项目信息与操作按钮整合为一个工作台面板。
- 顶部指标改为标签靠左、数字靠右的 KPI 状态条。
- 泳道增加左侧状态标识，看板列增加顶部状态色。
- 任务卡改为轻阴影、左侧状态线和右下状态点，减少模板化卡片感。
- 新增 `界面重设计说明-V0.8.2.md` 和 V0.8.2 截图。

### 关键判断
- 重设计只改视觉系统，不改数据结构和交互逻辑，保证之前完成的 Kanboard 静态复现能力不回退。
- 这版作为 Figma 高保真的优化后视觉基准，比 V0.8.1 更适合进入作品集展示。
- “美观”不等于装饰更多，而是状态规则更清楚、层级更稳定、操作更像真实工具。

### 验证结果
- `node tests/static-feature-audit-v08.js` 通过，共 79 项检查。
- `git diff --check` 通过。
- 390px 移动端宽度无页面级横向溢出。
- 关键颜色对比通过 4.5:1。

### 下一版本
V0.9：基于 V0.8.2 视觉基准进入 Figma 高保真原型。

## V0.8.1 - 静态界面视觉优化

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-02 |
| 状态 | 已完成 |
| 主题 | 降低静态页面 AI 味，提升真实产品工作台质感 |
| 主要文件 | `styles.css`、`界面视觉优化说明-V0.8.1.md`、`设计图/kanboard-v0.8.1-visual-polish.png`、`README.md` |

### 本轮新增
- 将整体视觉从单一青绿色模板感调整为中性后台基调，小面积使用蓝色强调主要动作。
- 优化侧栏、顶部指标、工具栏、看板列、任务卡、标签、弹窗和焦点态。
- 减轻卡片阴影和圆角，降低装饰感，提高工作台信息密度。
- 新增 V0.8.1 视觉优化截图和设计说明文档。

### 关键判断
- 当前阶段先优化本地静态页面，不进入 Figma；这样 Figma 高保真可以直接基于更成熟的视觉基准展开。
- 这次不改功能逻辑和数据结构，避免视觉优化影响已经通过验收的静态功能。
- “去 AI 味”的核心不是加更多视觉元素，而是减少平均化卡片、统一过重按钮和单一主色，让信息层级更像真实工具。

### 验证结果
- `node tests/static-feature-audit-v08.js` 通过，共 79 项检查。
- `git diff --check` 通过。
- 390px 移动端宽度无页面级横向溢出。
- 主文本、弱文本、按钮、侧栏文字、危险操作、状态标签的基础对比度均通过 4.5:1 标准。

### 下一版本
V0.9：进入 Figma 高保真原型，绘制原始路径、优化后路径和关键页面对比。

## V0.8 验收审计 - 静态功能全量检查

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-01 |
| 状态 | 已完成 |
| 主题 | 检查 V0.3-V0.8 已承诺静态功能是否全部实现 |
| 主要文件 | `tests/static-feature-audit-v08.js`、`静态功能验收报告-V0.8.md`、`README.md` |

### 本轮新增
- 新增 Playwright 静态功能验收脚本，覆盖项目、列、泳道、任务卡片、拖拽、视图、搜索、项目设置、分析、时间、自动化和通知。
- 新增 `静态功能验收报告-V0.8.md`，记录验收范围、通过项、静态模拟边界和进入 Figma 前的产品判断。
- 更新 README，将静态功能验收纳入项目产出物。

### 验证结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 79 项检查。

### 关键判断
- 当前静态页面已覆盖 V0.3-V0.8 承诺的用户侧核心功能，足够进入 Figma 高保真原型阶段。
- 登录、真实权限、数据库、后台定时任务、实时通知、附件、插件和 API 集成仍属于后端/系统级边界，不计入当前静态复现完成度。

## V0.8 - 分析时间与自动化补齐

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-01 |
| 状态 | 已完成 |
| 主题 | 补齐 Kanboard 项目分析、时间跟踪、循环任务、自动化规则和通知中心 |
| 主要文件 | `index.html`、`styles.css`、`app.js`、`分析时间与自动化补齐说明-V0.8.md`、`设计图/kanboard-v0.8-analytics-automation.png` |

### 本轮新增
- 顶部新增“项目分析”“自动化”“通知”入口。
- 项目分析展示打开任务、已关闭、预估工时、实际工时、循环任务、未读通知。
- 项目分析展示列分布、负责人时间分布和周期时间样例。
- 任务详情新增实际工时。
- 任务详情新增时间记录列表。
- 任务详情新增循环任务频率和下次生成日期。
- 自动化规则支持新增、启用、停用、删除和模拟执行。
- 通知中心支持展示通知和全部已读。

### 关键判断
- 自动化和通知在静态阶段只做本地模拟，不接真实后台任务和通知通道。
- 循环任务只保存规则，不自动生成任务，避免静态页面出现不可控定时行为。
- 时间跟踪采用“总实际工时 + 时间记录”的混合方式，既可快速记录，也能展示过程。

### 验证结果
- `node --check app.js` 通过。
- Playwright 验证实际工时、时间记录、循环任务、项目分析、自动化模拟、通知已读均通过。
- 移动端 390px 宽度下项目分析弹窗无横向溢出。
- 生成 V0.8 项目分析截图。

### 下一版本
V0.9：进入 Figma 高保真原型，输出优化前后对比、模板创建向导流程和作品集展示稿。

## V0.7 - 项目配置与协作补齐

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-01 |
| 状态 | 已完成 |
| 主题 | 补齐 Kanboard 项目设置、成员角色、分类标签、自定义筛选和泳道配置 |
| 主要文件 | `index.html`、`styles.css`、`app.js`、`项目配置与协作补齐说明-V0.7.md`、`设计图/kanboard-v0.7-project-settings.png` |

### 本轮新增
- 顶部新增“项目设置”入口。
- 增加项目类型：团队项目 / 个人项目。
- 增加默认泳道设置。
- 增加成员与角色管理：管理员、成员、访客。
- 成员进入负责人筛选和任务负责人输入建议。
- 增加分类管理，分类进入筛选和输入建议。
- 增加标签管理。
- 增加自定义筛选管理，筛选语法会实际影响看板过滤。
- 增加泳道排序、禁用、启用。

### 关键判断
- 静态阶段只表达权限配置结构，不做真实鉴权。
- 自定义筛选复用 V0.6 高级搜索语法，减少规则复杂度。
- 禁用泳道不删除任务，只影响视图展示。
- 默认泳道不能禁用，保证新增任务始终有落点。

### 验证结果
- `node --check app.js` 通过。
- Playwright 验证项目设置保存、成员筛选、分类筛选、自定义筛选、泳道禁用均通过。
- 移动端 390px 宽度下项目设置弹窗无横向溢出。
- 生成 V0.7 项目设置截图。

### 下一版本
V0.8：补齐分析、时间跟踪和自动化，包括项目分析页、任务时间跟踪、循环任务、自动化规则和通知中心静态模拟。

## V0.6 - Kanboard 完整复现补齐

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-01 |
| 状态 | 已完成 |
| 主题 | 补齐 Kanboard 用户侧主要视图、任务动作和搜索能力 |
| 主要文件 | `index.html`、`styles.css`、`app.js`、`Kanboard完整复现差距清单-V0.6.md`、`设计图/kanboard-v0.6-fuller-reproduction.png` |

### 本轮新增
- 增加视图切换：看板视图、列表视图、项目概览。
- 增加卡片显示模式：展开、折叠、紧凑。
- 增加列显示控制：可隐藏/显示单个看板列。
- 增加显示已关闭任务开关。
- 增加任务关闭/重新打开。
- 增加任务复制。
- 增加任务移动到其他项目。
- 增加内部任务链接：阻塞、被阻塞、重复、相关、父任务、子任务。
- 增加轻量高级搜索：`assignee:PM`、`tag:MVP`、`status:open`、`status:closed`、`column:待办`、`lane:产品设计泳道` 等。
- 增加项目概览中的任务统计、列分布、负责人分布、高优先级任务。

### 关键判断
- V0.6 不进入 Figma，先继续补齐 Kanboard 静态复现。
- 当前复现仍然是“用户侧静态复现”，不做真实登录、权限鉴权、插件、API、数据库和后端任务。
- 本轮优先补“能演示、能讲清楚产品结构”的功能，项目配置、成员角色、自动化和分析继续放到后续版本。

### 验证结果
- `node --check app.js` 通过。
- Playwright 验证列表视图、概览视图、高级搜索、任务链接、关闭任务、显示已关闭、复制任务、跨项目移动均通过。
- 生成 V0.6 页面截图。

### 下一版本
V0.7：补齐项目配置与协作能力，包括项目设置页、成员、角色、权限、分类、标签、自定义筛选、泳道排序和禁用。

## V0.5 - 新手创建向导优化

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-01 |
| 状态 | 已完成 |
| 主题 | 在 Kanboard 复现版上加入模板化新建项目能力 |
| 主要文件 | `index.html`、`styles.css`、`app.js`、`新手创建向导说明-V0.5.md`、`设计图/kanboard-template-v0.5.png` |

### 本轮新增
- 新建项目弹窗升级为创建向导，支持“从模板开始 / 空白项目”两种方式。
- 增加模板列表和模板预览，创建前可看到列结构、泳道和示例任务。
- 内置 4 个模板：个人学习项目、求职准备项目、小团队迭代、Bug 跟踪。
- 个人学习项目模板使用用户前面确认的“待学 / 学习中 / 已学完”三列。
- 模板创建会真正生成项目、泳道、看板列和示例任务卡，并保存到 localStorage。
- 空白项目保留原 Kanboard 轻量创建路径，避免影响老用户效率。
- 生成 V0.5 页面截图，作为功能优化 Demo 证据。

### 关键判断
- V0.5 开始从“复现 Kanboard”转入“基于复现版优化 Kanboard”。
- 向导嵌入原“新建项目”入口，而不是新开独立页面，保持轻量和可对比。
- 模板先做内置数据，不做模板编辑器，把范围控制在 P0。

### 验证结果
- `node --check app.js` 通过。
- Playwright 验证模板创建、空白创建、localStorage 写入均通过。
- 移动端 390px 宽度下无横向溢出。

### 下一版本
V0.6：继续补齐 Kanboard 用户侧静态复现能力，再进入 Figma 高保真。

## V0.4 - 静态页面增强复现

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-01 |
| 状态 | 已完成 |
| 主题 | 补齐更接近 Kanboard 的静态页面能力 |
| 主要文件 | `index.html`、`styles.css`、`app.js`、`静态页面完成说明-V0.4.md`、`设计图/kanboard-static-v0.4.png` |

### 本轮新增
- 增加泳道能力：新增、编辑、删除泳道。
- 看板从单层列结构升级为“泳道 × 列”的二维结构。
- 任务卡增加子任务 checklist、评论、活动记录和预估工时。
- 卡片增加子任务进度条，能在看板上快速判断完成程度。
- 支持卡片跨列、跨泳道流转。
- 支持卡片同列上移/下移，用于表达优先级。
- 筛选区增加泳道筛选。
- 生成 V0.4 页面截图，作为静态 Demo 证据。

### 关键判断
- 静态页面负责“可运行交互”，Figma 后续负责“高保真表达”。
- 完整复现不是把 Kanboard 的所有工程系统搬过来，而是优先复现产品经理能讲清楚的核心体验。
- 登录、权限、插件、自动化、附件上传等系统能力暂不进入静态页。

### 待验证问题
- 泳道 × 列的二维展示在小屏下是否需要更强的折叠能力。
- 子任务、评论、活动记录是否已经足够支撑作品集演示。
- V0.5 模板创建向导应嵌入当前新建项目弹窗，还是拆成独立向导页面。

### 下一版本
V0.5：在当前静态页面的新建项目入口上加入“空白项目 / 从模板开始”、模板选择、模板预览和示例任务卡。

## V0.3 - Kanboard MVP 复现

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-01 |
| 状态 | 已完成 |
| 主题 | 复现 Kanboard 核心看板体验 |
| 主要文件 | `index.html`、`styles.css`、`app.js`、`复现范围说明-V0.3.md`、`项目实战路线图.md`、`设计图/kanboard-mvp-v0.3.png` |

### 本轮新增
- 根据用户判断，将复现核心收敛为：项目、任务卡片、卡片流转。
- 调整项目路线：V0.3 先复现 Kanboard MVP，后续再加入新手创建向导优化。
- 实现可运行静态前端原型，支持本地打开。
- 支持项目创建、切换、编辑、删除。
- 支持看板列新增、编辑、删除和 WIP 限制展示。
- 支持任务卡创建、编辑、删除和拖拽流转。
- 支持任务字段：标题、描述、负责人、分类、优先级、截止日期、标签、颜色。
- 支持搜索、负责人筛选、分类筛选和项目指标展示。
- 使用 localStorage 保存数据，刷新页面后保留状态。
- 生成 V0.3 页面截图，作为作品集迭代证据。

### 关键判断
- 复现版先使用静态前端，降低工程复杂度，让学习重点落在产品结构和交互逻辑上。
- V0.3 不加入模板向导，避免在复现还没完成时混入优化功能。
- 数据结构采用“项目 -> 列 -> 任务卡片”，对应 Kanboard 的核心产品模型。

### 待验证问题
- 只做本地存储是否足够支撑作品集演示。
- 拖拽只支持跨列移动，是否需要补同列排序。
- 是否需要继续增加附件、报表等更接近 Kanboard 的功能。

### 下一版本
后续版本：在静态复现版的新建项目入口上加入新手创建向导、内置模板和示例任务卡。

## V0.2 - 调研分析

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-01 |
| 状态 | 已完成 |
| 主题 | 竞品分析、用户画像、痛点拆解与需求池 |
| 主要文件 | `项目实战路线图.md`、`竞品分析报告-V0.2.md`、`用户研究与需求池-V0.2.md` |

### 本轮新增
- 明确本项目从 V0.1 到 V1.0 的实战路线。
- 对比 Kanboard、WeKan、Trello、Notion 的新手创建体验。
- 建立 3 类目标用户画像：作品集准备中的学生、个人开发者、轻量团队负责人。
- 拆解创建项目、拆任务、理解看板、团队协作四类痛点。
- 形成 10 条需求池，并确定新手创建向导的 P0 范围。

### 关键判断
- Kanboard 的优势是轻量，不应把它改造成复杂协作套件。
- 新手真正需要的是“从目标到可用看板”的桥梁，而不是更多高级功能。
- P0 应聚焦新建项目入口、场景模板、列结构、示例卡片和创建前预览。
- 空白创建必须保留，避免影响高级用户效率。

### 待验证问题
- 4 个内置模板是否覆盖最高频的新手场景。
- 示例卡片数量控制在 3-5 张是否合适。
- 模板创建向导是否会让原本极简的 Kanboard 显得变重。

### 下一版本
后续进入新手创建向导优化：输出 4 个模板的具体内容、新手创建向导流程图、核心页面说明和用户故事。

## V0.1 - 选题基线

| 项目 | 内容 |
|---|---|
| 日期 | 2026-05-31 |
| 状态 | 已建立基线 |
| 主题 | Kanboard 新手项目创建与任务拆解体验优化 |
| 主要文件 | `README.md`、`Kanboard新手项目创建与任务拆解优化-启动说明.md` |

### 本轮新增
- 从 GitHub 经典开源产品中筛选传统产品优化对象。
- 确定 Kanboard 作为项目1的优化对象。
- 明确优化范围：新手从空白项目到第一张可用看板的关键路径。
- 增加版本迭代官，负责 Git/GitHub 可恢复性和版本记录。

### 关键假设
- 初次使用看板工具的用户需要模板和引导，而不是完全空白的创建入口。
- “新手项目创建向导”可以降低任务拆解门槛。
- 保留空白项目入口可以避免影响老用户效率。

### 待验证问题
- Kanboard 当前新建项目路径是否已经有模板或插件能力。
- 新手更需要“场景模板”还是“方法教学”。
- 看板模板是否应默认创建示例卡片，还是只创建列结构。

### 下一版本
V0.2：完成 Kanboard / WeKan / Trello / Notion 的竞品分析，沉淀用户痛点和机会点。
