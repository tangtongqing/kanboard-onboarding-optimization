# Kanboard 开发者集成补齐说明 V0.8.23

## 本轮目标
继续对照 Kanboard 官网补齐系统级能力，本轮聚焦开发者与外部集成入口：Webhook、JSON-RPC API、插件开发骨架。它们不属于单个项目配置，而是 Kanboard 作为可扩展开源工具的实例级能力。

## 官网依据
- Webhooks：官方说明 Webhook 会在事件发生时同步向外部 URL 发送 JSON POST，请求 token 放在 query string，接收端应在 1 秒内响应，否则会拖慢 UI。参考：https://docs.kanboard.org/v1/dev/webhooks/
- API：Kanboard 使用 JSON-RPC 2.0，并区分 Application API 与 User API，方法覆盖项目、看板、任务、子任务、用户、组、文件和 metadata 等对象。参考：https://docs.kanboard.org/v1/api/
- 插件注册：插件放在 `plugins` 目录，入口文件为 `Plugin.php`，类命名空间遵循 `Kanboard\Plugin\Yourplugin`，并扩展 `Kanboard\Core\Plugin\Base`。参考：https://docs.kanboard.org/v1/plugins/registration/
- 插件 hooks：插件可注册 API 方法、命令、任务过滤器、模板 hook、asset hook 和应用 hook。参考：https://docs.kanboard.org/v1/plugins/hooks/
- 插件安装：Web UI 安装插件默认关闭，需要可写插件目录、PHP Zip、`PLUGIN_INSTALLER=true`，并由管理员承担插件合法性校验。参考：https://docs.kanboard.org/v1/admin/plugins/
- Metadata：插件可通过 metadata 为任务、项目等对象保存扩展字段。参考：https://docs.kanboard.org/v1/plugins/metadata/

## 本轮新增
- 顶部新增 `开发者` 入口，放在 `部署检查` 与 `运维任务` 之间，表示“实例已能访问后，开始接入外部系统和二次开发”。
- 新增 `state.developer.webhooks`，保存 Webhook 启用状态、接收 URL、token、事件、响应预算、私网访问开关、接收状态和投递日志。
- 新增 Webhook payload 预览，包含 `event_name`、`event_data`、任务字段、请求头和 query token。
- 新增 Webhook 风险检查：开启但缺 URL/token、接收端预算超过 1 秒、允许私网访问带来的 SSRF 风险。
- 新增 `state.developer.api`，保存 endpoint、Application/User API 类型、认证方式、用户名、自定义 Header、当前 procedure、批量请求模式和调用日志。
- 新增 API procedure 索引，按 Application、Project、Board、Task、Subtask、User、Group、Metadata、File 分组展示常见方法。
- 新增 JSON-RPC 请求预览，支持单个请求和批量请求，并展示模拟响应。
- 新增 `state.developer.pluginDev`，保存插件目录名、namespace、作者、版本、兼容版本、homepage、schema、metadata、API method、hook、监听事件和生成时间。
- 新增 `Plugin.php` 骨架预览，表达插件目录结构、`Plugin extends Base`、`initialize()`、hook 注册、API method 注册、事件监听和 `getCompatibleVersion()`。
- 将 localStorage key 升级为 `kanboard-static-v0823`，并将项目 JSON 导出版本同步为 `kanboard-static-v0823`。

## 产品判断
- Webhook 与 API 都是“外部系统对接”能力，但方向相反：Webhook 是 Kanboard 主动通知外部系统，API 是外部系统主动调用 Kanboard。
- Webhook 必须突出“同步”和“1 秒内响应”，因为这是官网明确给出的使用风险，产品界面应该把风险前置，而不是隐藏在文档里。
- 插件开发不应只做“插件列表”，还要让用户理解目录结构、入口类、兼容版本、hooks 和 metadata，这样才能体现 Kanboard 的可扩展性。
- 本轮仍然是静态复现，不真实发送 HTTP 请求、不保存真实 token、不执行 PHP 插件代码，只表达配置状态、风险反馈和运行结果模拟。

## 验收结果
- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 264 项检查。
- 生成截图：`设计图/kanboard-v0.8.23-developer-integrations.png`。

## 下一步
继续对照官网剩余能力检查差距：平台化安装教程、OAuth2/外部认证提供者、自动动作扩展、通知类型扩展、更多插件开发子能力，以及是否需要把系统级配置整理为更完整的管理员信息架构。Figma 高保真原型仍放在完整静态复现进一步收敛后进入。
