# 运维任务与后台执行补齐说明 V0.8.20

## 本轮目标

本轮继续补齐 Kanboard 官网中的运维执行层能力，覆盖管理员 CLI、每日 Cronjob、Background Worker、邮件配置和运行日志。V0.8.19 已经表达了系统配置，本轮重点表达“这些配置如何被日常任务真正使用起来”。

## 官网依据

- Cronjob 文档说明：Kanboard 需要每天运行后台任务，用于项目每日统计、发送逾期任务通知、触发任务每日自动动作；Unix/Linux 常见命令是 `./cli cronjob`，Windows 可通过任务计划程序运行，也可以通过带 webhook token 的 URL 触发。
- CLI 文档说明：Kanboard 提供本机命令行工具，可运行 `cronjob`、`worker`、`job`、`notification:overdue-tasks`、`projects:daily-stats`、`trigger:tasks`、`db:migrate`、`db:version`、`plugin:upgrade`、`user:reset-2fa` 等命令。
- Background Workers 文档说明：Worker 可将发送邮件、外部通知等耗时任务交给队列处理；该能力已不再维护，需要自行承担风险；可选队列包括 Beanstalk、RabbitMQ，并需要进程管理器守护。
- Email 文档说明：Kanboard 支持 SMTP、Sendmail、PHP 内置 mail 函数和插件提供的其他传输方式；要发送邮件通知，用户需要开启通知、填写有效邮箱，并且是项目成员；应用 URL 需要配置，否则邮件链接可能默认指向 localhost。
- 配置文件文档说明：邮件传输、SMTP 主机、端口、加密、发件地址、Sendmail 命令、数据库迁移等都可通过 `config.php` 或环境变量配置。

参考链接：
- https://docs.kanboard.org/v1/admin/cronjob/
- https://docs.kanboard.org/v1/admin/cli/
- https://docs.kanboard.org/v1/admin/workers/
- https://docs.kanboard.org/v1/admin/email/
- https://docs.kanboard.org/v1/admin/config/

## 本轮新增

- 顶部新增 `运维任务` 入口。
- 新增 `state.operations.cron`，记录 Cronjob 执行方式、Cron 表达式、Windows 任务名、Cron URL、最后运行时间和状态。
- 新增 `state.operations.worker`，记录 Worker 是否启用、队列驱动、Supervisor、处理数量和失败数量。
- 新增 `state.operations.mail`，记录邮件传输方式、From、SMTP 主机、端口、加密方式、SMTP 用户、应用 URL、测试收件人和测试邮件时间。
- 新增 `state.operations.cli`，记录 CLI 当前选择命令和运行日志。
- 新增后台任务队列：项目每日统计、逾期任务通知、每日任务触发器、插件升级检查。
- 支持模拟运行 Cronjob，一次性完成每日后台任务并写入 CLI 日志。
- 支持模拟 Worker 处理一个队列任务，并更新处理计数。
- 支持模拟发送测试邮件，并记录最后测试时间。
- 支持选择 CLI 命令并生成命令预览。
- 支持模拟执行 CLI 命令，记录运行结果和时间。
- 将 localStorage key 升级为 `kanboard-static-v0820`，将项目 JSON 导出版本同步为 `kanboard-static-v0820`。

## 产品判断

Kanboard 的运维能力不是给普通项目成员使用的功能，而是让管理员知道“系统任务是否正常运行”的控制台。对产品复现来说，不能只列 CLI 命令，还要让用户看到任务状态、队列、邮件发送和执行日志。

本轮把运维执行层拆成四块：

1. Cronjob：解决每日统计、逾期通知和自动动作触发。
2. Worker：表达可选的队列执行能力，以及“已不再维护”的谨慎使用边界。
3. Mail：表达通知邮件从配置到测试的链路。
4. CLI：表达本机命令行命令、预览和执行日志。

这个设计能帮助你理解后台产品常见的“配置、调度、执行、日志”闭环。

## 静态复现边界

- 不执行真实 `./cli` 命令。
- 不创建真实 Cronjob 或 Windows 任务计划。
- 不连接 Beanstalk、RabbitMQ 或任何真实队列。
- 不发送真实邮件。
- 不执行数据库迁移。
- 不更新真实插件。
- 只在静态页面中模拟任务状态、队列推进、邮件测试和 CLI 日志。

## 验收结果

- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 201 项检查。
- 已生成 `设计图/kanboard-v0.8.20-operations-default.png`。
- 已生成 `设计图/kanboard-v0.8.20-operations-run-log.png`。

## 下一步

继续补齐 Kanboard 剩余系统级能力：数据库配置、SQLite/MySQL/PostgreSQL、性能优化、附件存储路径、升级流程、安装环境检查等。Figma 高保真原型继续放在静态复现差距进一步收敛之后。
