# 界面视觉优化说明 V0.8.1

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-02 |
| 目标 | 降低静态页面的模板感和 AI 味，让 Kanboard 复现版更像真实产品工作台 |
| 范围 | `styles.css`、设计截图 |
| 不改变 | 功能逻辑、数据结构、验收脚本、Kanboard 核心交互 |

## 这次为什么要改

上一版功能已经完整，但视觉上有几个问题：

- 同一种青绿色使用过多，页面容易显得像一键生成模板。
- 卡片、指标、筛选区、控制区都采用相似的大圆角卡片，层级有点平均。
- 按钮和标签普遍偏重，真实后台工具感不够。
- 看板列和任务卡都带明显装饰感，信息密度偏低。

这次的设计目标不是做得更炫，而是更像一个能长期工作的项目管理工具：清楚、克制、可扫描。

## 主要优化

| 区域 | 调整 | 设计判断 |
|---|---|---|
| 全局色彩 | 改为中性灰背景、石墨侧栏、小面积蓝色强调 | 减少单一主色支配页面，避免模板化 |
| 侧栏 | 增强当前项目选中态，降低装饰色 | 侧栏承担导航，不抢工作区注意力 |
| 顶部指标 | 从大卡片改成更紧凑的状态条风格 | 指标用于扫描，不需要占据过多视觉重量 |
| 工具栏 | 增加统一容器和更细的边界 | 让筛选、视图切换像真实工作台控件 |
| 看板列 | 列背景更轻，边界更明确 | 列是工作流容器，不是展示卡片 |
| 任务卡 | 减轻阴影、缩小圆角、hover 时才强调 | 任务卡更安静，信息更容易读 |
| 标签与状态 | 从胶囊感改成较克制的小标签 | 降低“组件库拼贴”感觉 |
| 弹窗 | 统一圆角、阴影和焦点样式 | 保持编辑体验一致 |
| 可访问性 | 增加 `focus-visible`，检查关键颜色对比 | 保证键盘用户和低视力用户可用 |

## 验收结果

本轮执行：

```powershell
node tests/static-feature-audit-v08.js
git diff --check
```

结果：

- 79 项静态功能回归检查全部通过。
- 移动端 390px 宽度无页面级横向溢出。
- 主文本、弱文本、按钮、侧栏文字、危险操作、状态标签的基础对比度均通过 4.5:1 标准。

## 当前边界

这次只做静态页面视觉打磨，没有进入 Figma，也没有改功能逻辑。下一步进入 Figma 高保真时，可以把这版作为“优化后界面”的视觉基准，再补充：

- 原始 Kanboard 创建路径。
- 优化后模板创建路径。
- 看板页、项目设置、项目分析、任务详情的高保真页面。
- 优化前后对比说明页。
