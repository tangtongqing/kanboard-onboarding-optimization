# 泳道空列精简与流程状态升级说明 V0.8.7

| 项目 | 内容 |
|---|---|
| 日期 | 2026-06-02 |
| 状态 | 已完成 |
| 主题 | 增加泳道空列精简显示，并升级 PM 工作流和个人学习项目流程状态 |
| 主要文件 | `app.js`、`index.html`、`styles.css`、`tests/static-feature-audit-v08.js`、`设计图/kanboard-v0.8.7-lane-compact-workflow.png` |

## 本轮解决的问题

每个泳道复用同一套列是 Kanboard 的标准模型，但在流程列变多后，很多泳道会出现大量“暂无匹配任务”的空列。这样虽然逻辑正确，但视觉上会显得重复、冗长。

V0.8.7 增加 `隐藏空列` 显示开关，默认打开：每条泳道只显示当前泳道里有任务的列。关闭开关后，仍然可以恢复完整流程列，方便检查全流程结构。

## 显示规则

- 默认打开 `隐藏空列`。
- 如果某个列在当前泳道没有可见任务，但在其他泳道有任务，则当前泳道会收起这个空列。
- 如果某个列是全局完全空的新列，则仍然显示，避免用户新增列后找不到它。
- 该优化只影响显示，不改变列、泳道、任务或数据结构。

## PM 工作流状态检查

原流程把一些阶段合并在一起，例如 `评审排期`、`上线运营`。为了更贴近完整 PM 周期，本轮调整为 11 个状态：

1. 机会/需求池
2. 需求澄清
3. 方案设计
4. PRD 原型
5. 评审确认
6. 研发排期
7. 研发实现
8. 测试验收
9. 上线发布
10. 运营反馈
11. 复盘迭代

主项目现在有 4 条泳道、11 个流程状态、25 张任务卡。默认隐藏空列后，首页只渲染 17 个有内容的泳道列块，避免 4×11 的重复空列。

## 学习项目升级

个人学习计划项目从 V0.8.5 的轻量路线继续升级：

- 流程状态从 3 列升级为 6 列：待规划、资料学习、案例拆解、实操产出、复盘完善、已掌握。
- 泳道从 4 条升级为 7 条：产品认知、用户研究、需求分析、方案表达、协作交付、数据增长、作品集求职。
- 任务卡从 12 张升级为 19 张，补充产品方法论、用户画像、竞品分析、MVP 范围、上线检查、周复盘和面试表达。

## 验证结果

- `node --check app.js` 通过。
- `node --check tests/static-feature-audit-v08.js` 通过。
- `node tests/static-feature-audit-v08.js` 通过，共 89 项检查。
- 已生成截图：`设计图/kanboard-v0.8.7-lane-compact-workflow.png`。

## 产品判断

底层仍然保持标准看板模型：项目内所有泳道共享同一套流程列。显示层用“隐藏空列”降低视觉噪音，让用户先看到当前泳道真正有任务的阶段；需要完整检查流程时，再关闭开关查看全量列。
